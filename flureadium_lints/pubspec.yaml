name: flureadium_lints
description: Analyzer plugin with the Flureadium lint rules that flag test assertions which cannot fail.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  analysis_server_plugin: ^0.3.0
  analyzer: ^10.0.0

dev_dependencies:
  analyzer_testing: 0.2.1
  test: ^1.25.0
  test_reflective_loader: ^0.2.3
